<Window x:Class="HJMIS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="481" Width="823">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="80" />
            </Grid.RowDefinitions>
            
            <Grid Grid.Row="0" >
                <ListView Name="ListView1" >
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="姓名" Width="100" DisplayMemberBinding="{Binding ExaminerName}"/>
                            <GridViewColumn Header="性别" Width="100" DisplayMemberBinding="{Binding Sex}"/>
                            <GridViewColumn Header="身份证号" Width="200" DisplayMemberBinding="{Binding IdCardNumber}"/>
                        </GridView>
                    </ListView.View>
                </ListView>

            </Grid>
            
        </Grid>
    </Grid>
</Window>
